#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 * @argc: variable containing the number of arguments
 * @argv: character variable containing the string values of argc
 * Return: 0 on success
 */

int main(int argc, char *argv[])
{
	printf("%d\n", argc - 1);
	return (0);
}
